version: 1
# Intentionally fails: demonstrates request timeout behavior.

vars:
  base_url: https://httpbin.org

steps:
  delay_request:
    request:
      method: GET
      url: ${base_url}/delay/5
      timeout: 2
    validate:
      - eq: [$$.status_code, 200]
